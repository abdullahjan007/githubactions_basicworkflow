name: basic-test
on: [push]
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v5      # get your code onto the runner
     - run: echo "hello"              # run a shell command
